name: Deploy Static Site to GitHub Pages

on:
  schedule:
    # Monday 08:00 UTC, two nominal hours after the CV build's 06:00.
    # The gap is sized from measured queue delay, not build duration:
    # scheduled runs here start 50min-7h late, but delays correlate within
    # a week, so relative order holds. Do not tighten it.
    - cron: '0 8 * * 1'
  workflow_dispatch:
  workflow_run:
    workflows:
      - "Update ADS Publications List"
      - "Generate Figure Data"
      - "Update ADS Citation Metrics"
      - "Update All Timeline Plots"
      - "Convert PDFs to SVG"
    types:
      - completed

concurrency:
  group: deploy
  cancel-in-progress: true

jobs:
  build-and-deploy:
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'schedule' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: main # Checkout main branch, which contains the latest data

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20' # Use a Node.js version compatible with your project

      - name: Install Dependencies
        run: npm ci

      - name: Build Static Site
        run: npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./out

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
