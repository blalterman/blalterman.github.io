# Progress Log (blalterman.github.io)

Reverse-chronological session log. Newest entry on top. Written by /session:close (or a project /close), read by /session:start.

---

## 2026-08-19 14:25 (4d7c697)
- Session: 4d7c6977-f1d0-46fa-ab99-9b4d2b0925f8
- Commits: 3308c27, ff482e6
- Done: Adopted and then purged the academic-SEO dispatch. It targets this repo -- Highwire citation tags, ScholarlyArticle JSON-LD, per-paper pages -- but had been written into the global `~/.claude/dispatches` tier, so `3308c27` files it here per the three-tier model and `ff482e6` purges it, leaving git history as its durable home. The dispatch is complete: 12 ACs PASS, executed 2026-05-19 as `406100f` "feat(seo): add ScholarlyArticle JSON-LD on publication category pages". Pattern C was selected over A and D, narrowing the original four-item scope to JSON-LD on existing category pages. Pushing required a rebase over 10 automated commits (CV PDF, ADS publications, timeline plots); all 10 verified present as ancestors afterward.
- Decisions (DECIDED): the dispatch belongs in this repo rather than the global tier, because project-scoped work follows the three-tier model. Purge after adopting, since the record's durable home is this repo's history.
- Open threads: OI-5 -- `og:type: article` extensions for the ben and research subpages -- was DEFERRED per a 2026-05-19 decision and belongs in a separate content dispatch. It is named in `3308c27`'s commit message so it stays greppable now the file is gone. Doc sweep found 13 tier-2 candidates in 6 groups, 0 accepted, 0 rejected -- unadjudicated. `unlogged_dates` reports 2026-08-19, 2026-08-17, 2026-08-10; backfill was not run (it is a bulk write and was not requested).
- Next action: author the deferred OI-5 content dispatch, or backfill the three unlogged dates.
- Active dispatch: none
- Active handoff: none

## 2026-08-04
- Done: Fixed a 32-week silent failure in the citation pipeline and hardened against the failure class. `scripts/fetch_ads_citations_to_data_dir.py` cached on file `st_mtime` with a 7-day window; `actions/checkout` resets mtime on every run, so the branch was unconditionally true and its ADS query never executed in CI (run log 30780127739: "Cached data ... is less than 7 days old. Skipping download."). `public/data/citations_by_year.json` had been frozen since `870870b` (2025-12-26); the site served 1 citation for 2026 against a true 208. It hid because matplotlib stamps `<dc:date>` into every SVG, so each weekly run produced a byte-different file and a real commit recording nothing. Second defect: `update_annual_citations.yml` staged only `citations_by_year.svg`, dropping `citations_by_year_dark.svg` and both PNGs, leaving the dark chart stale since 2026-03-09. Replaced the fetch with `scripts/derive_citations_by_year.py` (reshapes `ads_metrics.json['histograms']['citations']`, no network, direct key indexing so an ADS rename raises, reconciles against `citation stats -> total number of citations`); added `scripts/verify_citations_derivation.py`; deleted the fetch script and `update_annual_citations.yml`, folding its steps into `update_plots.yml` and carrying over the `if: failure()` block that workflow lacked; pinned `svg.hashsalt` in `scripts/plot_config.py`; added `scripts/audit_deployed_data.py` + `.github/workflows/audit-deployed-data.yml` (Mondays 12:00 UTC) and `.github/workflows/remind-pipeline-followup.yml` (2026-08-11 13:00 UTC). Commits: `ee25813`, `457702a`, `7504d0d`, `bb7248b`, `16fed2f` (CI), `9c4a8cd`, `4166630`. Live site verified serving 2026=208, total 1057, deployed SVGs byte-identical to `origin/main`; user visually confirmed the charts.
- Decisions (DECIDED): keep `<dc:date>` in SVGs for generation provenance, suppressing only the random element id, after the user split a change that had wrongly bundled the two; fix and deploy first, defer workflow consolidation (5 to 3) to issue #16; track follow-up via a scheduled GitHub comment rather than calendar or PM dashboard. (PROPOSED, not built): commit-gating on a date-normalized plot diff, deferred as low steady-state value since citations accrue most weeks.
- Open threads: Issue #16 (assigned, 5 items: consolidate workflows, pin `matplotlib` in `scripts/requirements.txt`, triage 15 open `workflow-failure` issues, untrack `scripts/__pycache__/*.pyc`, delete `public/data/citations_by_year.json.back` stub data). The reorganized `update_plots.yml` has run only via `workflow_dispatch`; its first natural `workflow_run` execution is Monday 2026-08-10, observed automatically by the audit. `Audit Deployed Data` has no heartbeat, so "no issue filed" and "the audit did not run" are indistinguishable. User's local `ADS_DEV_KEY` returns HTTP 401 while the GitHub secret works. Doc sweep: 17 candidates in 7 groups, 0 accepted, 17 rejected; at least 4 are demonstrable false positives (`ARCHITECTURE.md` has 16 references in `CLAUDE.md`, `docs/AGENTS.md` has 8), so the sweep is not resolving `CLAUDE.md` as a live root. Progress-log debt: 2026-08-03, 07-27, 07-20 are automation-only Mondays (7 bot commits each, no session); backfill declined as it would record nothing `git log` does not. Two process findings not yet fed back into the rule corpus: (1) ran `git log` as evidence against a clone 21 commits stale, the corpus-currency failure `negative-results-need-positive-controls.md` already names, caught only when CI logs contradicted local history; (2) `/reasoning-and-decisions:evaluate` carried cost pressure (part count as a deciding factor) but `/reasoning-and-decisions:reason sufficiency` carried none and tripled scope by inventing requirements then failing the recommendation for not covering them, corrected from five components to two after the user challenged it.
- Next action: Tuesday 2026-08-11, read the reminder comment on issue #16, which carries the audit verdict. If the audit passed, the gate is cleared and workflow consolidation can start with a clean baseline; if it failed, read which assertion broke before changing anything.
- Active LP: none
- Active handoff: none

## 2026-07-15
- Done: Shipped self-hosted per-publication PDF downloads. Added `public/data/publication-pdfs.json` (53-entry registry keyed by bibcode), joined in `loadAllPublications` (`src/lib/data-loader.ts`) with a build-time orphan-key positive control; added `pdfPath`/`pdfVersion` to `src/types/publication.ts`; added a version-labeled ("PDF"/"Preprint") Download button, centered in the links cell, in `src/components/publication-filters.tsx`. Copied 53 license-verified PDFs into `public/papers/pdfs/` at native resolution. Backfilled full author lists into `non_ads_publications.json` from Zenodo; removed the dead USRA link from the Brandt entry. Commits: `9e787f4`, `fbf1daa`, `cd469b5`, `7a254ab`, `6b6d0b8`, `962563e` (Decadal commit rebased onto the weekly automation commits). Deployed live via `workflow_dispatch` of `deploy.yaml` (run 29434958713, success). Coverage: refereed 39, white papers 13, thesis 1.
- Decisions (DECIDED): host all refereed + thesis + white papers at native resolution (no downsampling, no Git LFS — Pages does not serve LFS objects); label white-paper downloads "PDF" not "White Paper"; version-of-record hosted where CC-licensed (31), arXiv preprint where the VoR is publisher-copyright (8: 2 arXiv-CC + 6 author-right basis), thesis (1), white papers (13: 7 Zenodo CC-BY/CC0 + 6 BAAS Decadal CC-BY 4.0); PDF registry lives OUTSIDE the weekly-overwritten `ads_publications.json` and is joined by bibcode (validated under a real weekly ADS overwrite this session — zero orphans).
- Open threads: Brandt Helio2050 white paper (`NOADS-Helio2050ISP`) is the only unhosted white paper — no reachable source (USRA link dead 404, not on Zenodo/BAAS); awaiting a user-supplied PDF copy. AAS/BAAS/MDPI block `urllib`/WebFetch (403/Cloudflare); the working technique is fetching via the real Chrome browser (`mcp__claude-in-chrome` fetch-in-page).
- Next action: When the user provides the Brandt PDF, copy it to `public/papers/pdfs/NOADS_Helio2050ISP.pdf`, add its `publication-pdfs.json` entry (`version: whitepaper`), backfill its ~91-author list, build, commit, push, then re-trigger `deploy.yaml` (`gh workflow run deploy.yaml --ref main`).
- Active LP: none
- Active handoff: none
